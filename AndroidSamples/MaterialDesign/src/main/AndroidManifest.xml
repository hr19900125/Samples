<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sc.samples">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".HomeActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".example.ActLifeCycleActivity" />
        <activity android:name=".example.ActLifeCycle2Activity" />
        <activity android:name=".example.FragLifeCycleActivity" />
        <activity android:name=".example.MoveViewActivity"/>
        <activity android:name=".example.TouchEventDispatch1Activity"/>
    </application>

</manifest>
